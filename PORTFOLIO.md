# Portfolio Notes

## Resume bullet

Built a Unix-style shell in C supporting process creation and execution, pipelines, background jobs, built-in commands, command history, and parent-child synchronization using Linux process and file-descriptor primitives.

## Interview topics

- `fork()` and `execvp()`
- Pipes and `dup2()`
- Foreground/background process management
- `waitpid()` and zombie prevention
- Built-in command handling
- Command history and execution timing
